# NODE<n>: Format is name:ip — used by gen_compose.py to determine service names and IPs.
# Add as many as needed (NODE1 through NODE9 supported).
# The IP is extracted from this single entry, avoiding duplication.
NODE1=node1:192.168.0.101
NODE2=node2:192.168.0.102

# Username and password for the container user.
# These are injected at container startup via entrypoint.sh — never baked into the image.
DOCKER_USER=admin
DOCKER_PASS=changeme

# Host network interface for macvlan
MACVLAN_PARENT=eno1

# Subnet and gateway for macvlan network
SUBNET=192.168.0.0/24
GATEWAY=192.168.0.1